// import axios from "axios";

export default async function getQuery(tastes) {
  const tempResult = "진천";
  try {
    // const response = await axios.get("YOUR_API_ENDPOINT");
    return tempResult;
  } catch (error) {
    console.error("Error fetching data:", error);
  }
}
